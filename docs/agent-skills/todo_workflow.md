---
name: todo_workflow
description: 需要多个相互依赖步骤、先调查再执行或持续更新当前进度时使用；单步回复、长期目标和跨重启项目管理不要使用。
---

# Todo 工作流

遇到多步工作时，先用 `todo action=update` 写一个短计划。计划只服务当前进程内的短期工作，不是长期记忆。

规则:

- 每条 todo 只写一个动作。
- 同一时间最多一个 `in_progress`。
- 开始做哪一步，就把那一步设为 `in_progress`。
- 完成一步后，立刻 `todo action=update` 标成 `completed`，再把下一步设为 `in_progress`。
- 如果任务变了，提交完整的新列表，不要只补一条增量。

适合:

- 读多个文件后再判断。
- 需要先查工具、再执行、再验证。
- 用户要求“先分析/对照/优化”这种容易跑偏的任务。

不适合:

- 单句聊天回复。
- 长期目标、跨重启项目管理。
- 要记住某个人或某个群的信息；那应该用 `memory`。
